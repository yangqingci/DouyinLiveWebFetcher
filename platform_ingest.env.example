# 弹幕推送配置模板。
# 第一次使用时复制为 platform_ingest.env，然后按实际环境修改。

# douyin 云端平台的弹幕接入地址。
# 默认值：http://127.0.0.1:8000/ingest，适合本地联调。
# 本地联调：填 http://127.0.0.1:8000/ingest。
# 服务器部署：填 http://服务器IP:8080/ingest，或你的域名地址。
INGEST_URL=http://127.0.0.1:8000/ingest

# 平台直播间页面里复制的用户 ingest token。
# 默认值：local-admin-ingest-token，需和 douyin/.env.local 里的 PLATFORM_ADMIN_INGEST_TOKEN 保持一致。
# 本地联调：可以使用 local-admin-ingest-token。
# 服务器部署：必须换成平台页面里该用户自己的 ingest token。
# 注意：这个不是登录 JWT，不要填登录 token。
INGEST_TOKEN=local-admin-ingest-token

# 抖音直播间 ID，通常是直播间 URL 末尾的数字。
# 默认值：your_douyin_live_id，占位值，必须修改。
# 作用：DouyinLiveWebFetcher 用它连接指定抖音直播间。
# 示例：直播间地址是 https://live.douyin.com/123456789，则填 123456789。
DOUYIN_LIVE_ID=your_douyin_live_id

# 可选。不填时默认使用 DOUYIN_LIVE_ID。
# 作用：发送给 douyin 云端的 douyin_room_id。
# 本地联调：可以留空。
# 服务器部署：建议填写，并确保和平台里绑定的直播间 ID 一致。
# 如果填写错误，云端可能返回 403 Douyin room mismatch。
INGEST_ROOM_ID=
